# Taipo n-gram drills

Practice lists for the n-gram chords, generated by `words/drills.py` from the
18 `Action::Text` entries in `TAIPO_ACTIONS`.  The table is written easiest
chord first, which is also the order they are worth learning in, so the 13
lessons follow it.  Regenerate with:

```
uv run words/drills.py > docs/taipo-drills.md
uv run words/drills.py --files
```

See [ngrams-results.md](ngrams-results.md) for where the grams came from and
which chord each one sits on.

## How to use these

Every drill is also a file in [drills/](drills/), one line, no newlines --
MonkeyType's custom text treats a newline as a character to type, so the
wrapped blocks below are for reading and the files are what to paste:

```
pbcopy < docs/drills/01a-the.txt
```

Then MonkeyType: **custom text**, paste, turn *random* on and set a word count.
Punctuation and numbers off; its own capitalisation option is the way to reach
the `+Sp` half of a chord, so the lists are all lower case.

In 5 of the 13 lessons two chords arrive together, because `ing`, `and`,
`ation`, `for`, `ent` each take a third to a half of what the gram before them
would otherwise type.  Learning `in` by itself would train `in`+`g` across a
third of its uses, for `ing` to undo later.  The third list in those lessons
mixes the pair, which is the drill that matters: the same `in` is the whole
answer in one word and the front of `ing` in the next.

Nothing outside the keyboard can see whether a word was chorded or spelled out,
so the lists do the enforcing.  A word is in a lesson only if two things hold:
no chord past it would type the word any cheaper, and no chord before it
already does.  So while typing a lesson, **every word in front of you has
exactly one best answer and it is today's chord** -- if you spell one out you
will know, because you had no reason to be there.  `nation` is a `tion` word
and never an `on` word; `more` is not an `or` word, because `re` came first and
`m o re` already costs three.

Speed is the wrong measure at first.  A chord you have to think about is slower
than the letters it replaces, and stays slower for a while; the drill is for
the recognition, not the pace.  Watch instead for whether the gram is being
*seen* -- `otherwise` as `o-the-r-w-i-s-e` rather than nine letters.

A review list appears every 3 lessons and at the end, mixing everything learned
so far.  Those are where the chords stop being an exercise: the words are
ordinary, the grams are wherever they fall, and nothing marks them.

Each lesson's words are the most common qualifying ones from MonkeyType's
`english_10k`; `-n` takes more per lesson if a list goes stale.
## Lesson 1 — `the`

`the` — 45 words in the list qualify, 40 here, most common first.

`drills/01a-the.txt`

```
the other they their these them then others further whether weather either together
rather mother otherwise southern father leather therapy theme brother clothes neither
brothers ethernet theft thesis authentic mothers synthesis heather therapeutic gather
synthetic thesaurus theology luther hypothesis motherboard
```

## Lesson 2 — `in` and `ing`

`in` alone — 272 words in the list qualify, 40 here, most common first.

`drills/02a-in.txt`

```
in find into info links line within index link think since main include print point join
windows again login window included points magazine domain income mind casino inside
fine kind machine inn win medicine involved wine joined input bin skin
```

`ing` alone — 368 words in the list qualify, 40 here, most common first.

`drills/02b-ing.txt`

```
using shipping being following during shopping going looking things single something
building making meeting learning marketing having living thing getting writing kingdom
doing king nothing running clothing taking coming spring wedding warning providing
housing bring trying teaching leading ring mailing
```

Both, mixed — the drill that matters, where the same `in` is the whole answer
in one word and the front of `ing` in the next.

`drills/02c-in-ing.txt`

```
including in using training find shipping beginning into being thinking info following
finding links during printing line shopping binding within going winning index looking
dining link things painting think single findings since something drinking main building
mining include making involving
```

## Lesson 3 — `er`

`er` — 513 words in the list qualify, 40 here, most common first.

`drills/03a-er.txt`

```
here were service over her number after where very user under university member terms
members power computer per property every however better server never users offers paper
poker seller offer delivery term computers powered period average ever cover player
newsletter
```

### Review through lesson 3: `the` `in` `ing` `er`

100 words.

`drills/03b-review.txt`

```
the in using here other find shipping were they into being service their info following
over these links including her them line during number then internet shopping after
others within training where further index going very whether link looking user weather
think things under either since single university together main something member rather
include building terms mother print making members otherwise point meeting power
southern join learning computer father windows marketing per leather again having
property therapy login living every theme window thing however brother included getting
better clothes points everything server neither magazine advertising never brothers
domain writing users
```

## Lesson 4 — `an` and `and`

`an` alone — 244 words in the list qualify, 40 here, most common first.

`drills/04a-an.txt`

```
an can any than company many want change man advanced plan human language range means
bank thanks loan loans mean plans exchange woman channel wanted thank significant van
panel plant dance grant fan changed clean advance chance fantasy santa guarantee
```

`and` alone — 52 words in the list qualify, 40 here, most common first.

`drills/04b-and.txt`

```
and hand land island brand islands band command grand brands hands demand random
thousands handle broadband expand husband bands landscape thousand commands sand
handbook expanded lands candy bandwidth demands handheld candle handed handy handled
homeland highland handbags sandwich handmade highlands
```

Both, mixed — the drill that matters, where the same `an` is the whole answer
in one word and the front of `and` in the next.

`drills/04c-an-and.txt`

```
an and can hand any land than island company brand many islands want band change command
man grand advanced brands plan hands human demand language random range thousands means
handle bank broadband thanks expand loan husband loans bands mean landscape
```

## Lesson 5 — `tion` and `ation`

`tion` alone — 89 words in the list qualify, 40 here, most common first.

`drills/05a-tion.txt`

```
section action options edition collection solutions function production protection
position selection solution addition option functions fiction auction motion actions
dictionary sections competition auctions election nutrition positions subscription
promotion evolution collections execution acquisition completion subscriptions elections
exception adoption detection productions editions
```

`ation` alone — 117 words in the list qualify, 40 here, most common first.

`drills/05b-ation.txt`

```
education location application association applications communications foundation
communication population navigation publications accommodation locations publication
vacation nation situation nations specifications legislation participation
classification notification specification duration aviation telecommunications vacations
situations simulation optimization nationwide immigration occupation associations
radiation medication violation expectations obligation
```

Both, mixed — the drill that matters, where the same `tion` is the whole answer
in one word and the tail of `ation` in the next.

`drills/05c-tion-ation.txt`

```
section education action location options application edition association collection
applications solutions communications function foundation production communication
protection population position navigation selection publications solution accommodation
addition locations option publication functions vacation fiction nation auction
situation motion nations actions specifications dictionary legislation
```

## Lesson 6 — `re`

`re` — 391 words in the list qualify, 40 here, most common first.

`drills/06a-re.txt`

```
are more free software review read research reviews results address area care three
pictures children return previous reply compare press credit thread required red result
release picture areas future share sure direct resource received receive hardware figure
fire score returns
```

### Review through lesson 6: `the` `in` `ing` `er` `an` `and` `tion` `ation` `re`

100 words.

`drills/06b-review.txt`

```
the in using service an and section education are other find shipping over can hand
action location more they into being her any land options application free their info
following number than island edition association there these links including after
company brand collection applications here them line during very many islands solutions
communications were then internet shopping user want band function foundation software
another within training under change command production communication where others index
going university insurance grand protection population review further link looking
member man brands position navigation read whether think things terms advanced hands
selection operations research weather
```

## Lesson 7 — `or` and `for`

`or` alone — 210 words in the list qualify, 40 here, most common first.

`drills/07a-or.txt`

```
or world work support sports network north author memory password major color short word
works words sort port mortgage airport editor prior sport favorite worldwide networks
opportunity horse authority door floor outdoor authors born keywords sector worth poor
lord visitors
```

`for` alone — 32 words in the list qualify, 32 here, most common first.

`drills/07b-for.txt`

```
for before forum form forums force foreign forms forward ford effort efforts fort forgot
forced comfort forget affordable formula forth comfortable formed uniform fortune afford
fork forty forge formulas uniforms forks forgive
```

Both, mixed — the drill that matters, where the same `or` is the whole answer
in one word and the tail of `for` in the next.

`drills/07c-or-for.txt`

```
workforce or for world before work forum support form sports forums network force north
foreign author forms memory forward password ford major effort color efforts short fort
word forgot works forced words comfort sort forget port affordable mortgage formula
airport
```

## Lesson 8 — `es`

`es` — 426 words in the list qualify, 40 here, most common first.

`drills/08a-es.txt`

```
message best does games design prices pages access resources times sites movies process
west yes test articles issues less activities files images messages provides miles rules
archives makes policies cases designed countries shoes notes supplies types necessary
names facilities courses
```

## Lesson 9 — `en` and `ent`

`en` alone — 208 words in the list qualify, 40 here, most common first.

`drills/09a-en.txt`

```
when been send between even women open end teen science men friend given garden friends
written often taken license enough agency seen benefits menu teens length friendly
evidence kitchen sense enjoy benefit ten seven enable defense avenue extended sequence
weekend
```

`ent` alone — 140 words in the list qualify, 40 here, most common first.

`drills/09b-ent.txt`

```
comments development current events department comment payment equipment event document
currently entry employment client sent went documents parents excellent agent century
rent agents parent elements clients scientific argument apartments improvement element
identify moment movement identified payments cent apartment departments spent
```

Both, mixed — the drill that matters, where the same `en` is the whole answer
in one word and the front of `ent` in the next.

`drills/09c-en-ent.txt`

```
amendment when comments dependent been development sentence send current enrollment
between events amendments even department engagement women comment enlargement open
payment commencement end equipment enjoyment teen event gentlemen science document
sentenced men currently friend entry given employment garden client friends
```

### Review through lesson 9: `the` `in` `ing` `er` `an` `and` `tion` `ation` `re` `or` `for` `es` `en` `ent`

100 words.

`drills/09d-review.txt`

```
the in using services an and section information are or for business when management
other find shipping service can hand description education more world before message
been center they into being over any land action location free work forum best send
comments their info following her than island questions application there support form
does between development these links including number company brand options association
here order forums research even current them line during after many islands question
applications were report performance games women government then internet shopping very
want band edition organization software sports force results open events another within
```

## Lesson 10 — `on`

`on` — 230 words in the list qualify, 40 here, most common first.

`drills/10a-on.txt`

```
on one only contact phone long control money second month million discussion once months
common electronics upon done front economic none among commission soon electronic season
someone union decision division contract button song telephone son mission television
songs wrong beyond
```

## Lesson 11 — `al`

`al` — 286 words in the list qualify, 40 here, most common first.

`drills/11a-al.txt`

```
all also health local special total small digital call shall quality value sale medical
legal social always journal called official technical talk usually global deals half
political although album actually capital allow male fall female physical deal valley
hall hospital
```

## Lesson 12 — `at`

`at` — 235 words in the list qualify, 40 here, most common first.

`drills/12a-at.txt`

```
that at what date data rate private updated database update watch chat death sat
appropriate saturday located late associated rated match fat cat flat path satellite
amateur graduate patch heat affiliate immediately separate approximately attack native
boat dedicated atom associate
```

### Review through lesson 12: `the` `in` `ing` `er` `an` `and` `tion` `ation` `re` `or` `for` `es` `en` `ent` `on` `al` `at`

100 words.

`drills/12b-review.txt`

```
the in using services an and section information are or for business when management on
all that other online shipping service can hand description international more world
before message been center one also at they find being over any land conditions
education free work forum best general comments only health what their into following
her than island action national there support form does send development contact real
date these info including number company brand questions location here order forums
research between current phone local data them links rating after many islands options
application were report performance games even government long
```

## Lesson 13 — `st`

`st` — 196 words in the list qualify, 40 here, most common first.

`drills/13a-st.txt`

```
first list just last most system post must posted systems posts still stock start cost
study staff east star listed least past district costs style fast step stuff stay stop
started lost artist custom stars lists abstract artists trust stage
```

### Everything: `the` `in` `ing` `er` `an` `and` `tion` `ation` `re` `or` `for` `es` `en` `ent` `on` `al` `at` `st`

100 words.

`drills/13b-review-all.txt`

```
the in using services an and section information are or for business when management on
all that first other online shipping service can standard description international more
world before message been center one also at list they find being over any hand
conditions education free work forum does general comments only health what just their
into following her than land action national there support form research send
development contact real date state these info including number company island questions
location here order forums games between current phone local data last them links rating
after many brand options application were report
```

