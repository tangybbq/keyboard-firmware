# Taipo n-gram drills

Practice lists for the n-gram chords, one lesson per chord, generated by
`words/drills.py` from the 13 `Action::Text` entries in `TAIPO_ACTIONS`.
The table is written easiest chord first, which is also the order they are
worth learning in, so the lessons follow it.  Regenerate with:

```
uv run words/drills.py > docs/taipo-drills.md
```

See [ngrams-results.md](ngrams-results.md) for where the grams came from and
which chord each one sits on.

## How to use these

MonkeyType: **custom text**, paste a block, turn *random* on and set a word
count.  Punctuation and numbers off.  The capital lists are separate because
custom text keeps case, and a lower-case list with `Random` on will never
exercise the `+Sp` half of a chord.

Nothing outside the keyboard can see whether a word was chorded or spelled out,
so the lists do the enforcing.  A word is in lesson `g` only if two things
hold: no chord past `g` would type it any cheaper, and no chord before `g`
already does.  So while typing a lesson, **every word in front of you has
exactly one best answer and it is today's chord** -- if you spell one out you
will know, because you had no reason to be there.  `nation` is a `tion` word
and never an `on` word; `more` is not an `or` word, because `re` came first and
`m o re` already costs three.

Speed is the wrong measure at first.  A chord you have to think about is slower
than the letters it replaces, and stays slower for a while; the drill is for
the recognition, not the pace.  Watch instead for whether the gram is being
*seen* -- `otherwise` as `o-the-r-w-i-s-e` rather than nine letters.

A review list appears every 3 lessons and at the end, mixing everything
learned so far.  Those are where the chords stop being an exercise: the words
are ordinary, the grams are wherever they fall, and nothing marks them.

Each lesson's words are the most common qualifying ones from MonkeyType's
`english_10k`.  `--lesson <gram>` prints one list bare, and `-n` takes more
words per lesson if a list gets stale.

## Lesson 1 — `the`

45 words in the list qualify; 40 here, most common first.

```
the other they their these them then others further whether weather either together
rather mother otherwise southern father leather therapy theme brother clothes neither
brothers ethernet theft thesis authentic mothers synthesis heather therapeutic gather
synthetic thesaurus theology luther hypothesis motherboard
```

Capitals (`The`, the `+Sp` twin):

```
The They Their These Them Then Therapy Theme
```

## Lesson 2 — `in`

673 words in the list qualify; 40 here, most common first.

```
in find into info links line using within shipping index being link following think
since including during shopping main include print training point join windows going
again looking things login single something building making meeting learning marketing
having living window
```

Capitals (`In`, the `+Sp` twin):

```
In Into Info Index Including Include Included Income
```

## Lesson 3 — `er`

513 words in the list qualify; 40 here, most common first.

```
here were service over her number after where very user under university member terms
members power computer per property every however better server never users offers paper
poker seller offer delivery term computers powered period average ever cover player
newsletter
```

Capitals (`Er`, the `+Sp` twin):

```
Erotic Era Erotica
```

### Review 1–3: `the` `in` `er`

100 words, using only these chords.

```
the in here other find were they into service their info over these links her them line
number then using after others internet where further within very whether shipping user
weather index under either being university together link member rather following terms
mother think members otherwise since power southern including computer father during per
leather shopping property therapy main every theme include however brother print better
clothes training server neither point never brothers join users ethernet windows offers
theft going paper gathering again poker thesis looking seller authentic things offer
mothers login delivery synthesis single term heather something computers therapeutic
```

## Lesson 4 — `an`

296 words in the list qualify; 40 here, most common first.

```
and an can any than company many want change man advanced plan human language range hand
means land island bank thanks loan brand loans mean plans exchange islands woman band
channel wanted thank command significant grand van panel plant brands
```

Capitals (`An`, the `+Sp` twin):

```
And An Any Anime Announced Angel Anyway Antique
```

## Lesson 5 — `tion`

206 words in the list qualify; 40 here, most common first.

```
education section location action application options edition association collection
solutions applications function production protection position selection solution
addition communications foundation communication population navigation option
publications accommodation locations publication vacation functions fiction auction
nation motion actions situation dictionary sections competition auctions
```

## Lesson 6 — `re`

403 words in the list qualify; 40 here, most common first.

```
are more free software review read research reviews before results address area care
three current pictures children return previous reply compare press credit thread
required red result release picture areas future share sure direct resource received
currently receive hardware figure
```

Capitals (`Re`, the `+Sp` twin):

```
Review Read Research Reviews Results Return Reply Required
```

### Review 1–6: `the` `in` `er` `an` `tion` `re`

100 words, using only these chords.

```
the in service and education are other find over an section more they into her can
location free their info number any action there these links after than application here
them line very company options were then using user many edition software another
internet under want association where others within university change collection review
further shipping member insurance solutions read whether index terms man applications
research weather being reserved advanced function reviews either link members plan
production before together following power human protection results rather think
computer language position address therefore since per range selection area mother
including property hand
```

## Lesson 7 — `or`

241 words in the list qualify; 40 here, most common first.

```
for or world work support forum sports network north form author forums memory password
major color short word works words sort force port mortgage airport editor forms prior
sport favorite worldwide networks opportunity horse forward authority door floor outdoor
authors
```

Capitals (`Or`, the `+Sp` twin):

```
Or Oracle Orbit Orchid
```

## Lesson 8 — `es`

426 words in the list qualify; 40 here, most common first.

```
message best does games design prices pages access resources times sites movies process
west yes test articles issues less activities files images messages provides miles rules
archives makes policies cases designed countries shoes notes supplies types necessary
names facilities courses
```

Capitals (`Es`, the `+Sp` twin):

```
Established Establish Escape Essays Essay Esteem Establishes
```

## Lesson 9 — `en`

351 words in the list qualify; 40 here, most common first.

```
when been comments development send between even women open end events department teen
science men friend comment payment equipment given event garden friends document written
often taken license enough entry agency employment seen benefits menu teens length
client sent went
```

Capitals (`En`, the `+Sp` twin):

```
End Enough Entry Enjoy Enable Ends Ended Enlarge
```

### Review 1–9: `the` `in` `er` `an` `tion` `re` `or` `es` `en`

100 words, using only these chords.

```
the in services and information are for business when other find service an education
more or message been they into over can section free world best management their info
her any location there work does center these links number than description here support
research comments them line after company action were order games development then using
very many questions software report results send another internet user want application
where forum design between others within under change options review sports address even
further shipping university insurance question read network reserved women whether index
member man edition reviews north prices open weather
```

## Lesson 10 — `on`

230 words in the list qualify; 40 here, most common first.

```
on one only contact phone long control money second month million discussion once months
common electronics upon done front economic none among commission soon electronic season
someone union decision division contract button song telephone son mission television
songs wrong beyond
```

Capitals (`On`, the `+Sp` twin):

```
On One Only Once Onto Onion
```

## Lesson 11 — `al`

286 words in the list qualify; 40 here, most common first.

```
all also health local special total small digital call shall quality value sale medical
legal social always journal called official technical talk usually global deals half
political although album actually capital allow male fall female physical deal valley
hall hospital
```

Capitals (`Al`, the `+Sp` twin):

```
All Also Always Although Album Allow Allows Allowed
```

## Lesson 12 — `at`

235 words in the list qualify; 40 here, most common first.

```
that at what date data rate private updated database update watch chat death sat
appropriate saturday located late associated rated match fat cat flat path satellite
amateur graduate patch heat affiliate immediately separate approximately attack native
boat dedicated atom associate
```

Capitals (`At`, the `+Sp` twin):

```
At Attack Atom Attempt Attached Attacks Attribute Athletic
```

### Review 1–12: `the` `in` `er` `an` `tion` `re` `or` `es` `en` `on` `al` `at`

100 words, using only these chords.

```
the in services and information are for business when on all that other online service
an international more or message been one also at they find over can education free
world best general only health what their into her any national there work does
management contact real date these info number than section here support research center
phone local data them links after company location were order games comments long
special great then line very many description software report results development
version total related another using user want conditions where forum design send control
personal water others internet under change
```

## Lesson 13 — `st`

196 words in the list qualify; 40 here, most common first.

```
first list just last most system post must posted systems posts still stock start cost
study staff east star listed least past district costs style fast step stuff stay stop
started lost artist custom stars lists abstract artists trust stage
```

Capitals (`St`, the `+Sp` twin):

```
Still Stock Start Study Staff Star Style Step
```

### Everything: `the` `in` `er` `an` `tion` `re` `or` `es` `en` `on` `al` `at` `st`

100 words, using only these chords.

```
the in services and information are for business when on all that first other online
service an international more or message been one also at list they find over can
education free world does general only health what just their into her any national
there work research management contact real date state these info number than section
here support games center phone local data last them links after company location were
order results comments long special great most then line very many description software
report design development version total states system another using user want conditions
where forum address send
```

